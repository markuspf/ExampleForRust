#
# ExampleForRust: Example package for GAP Kernel Module written in Rust
#
# Implementations
#
InstallGlobalFunction( ExampleForRust_Example,
function()
	Print( "This is a placeholder function, replace it with your own code.\n" );
end );

